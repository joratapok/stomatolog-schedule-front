export type SignInResponse = {
  accessToken: string;
  refreshToken: string;
};
export type StateDate = {
  date: number;
  dateText: string;
};
export type StateEventInit = {
  cabinetId: number;
  timeStart: number;
};
