import {styled} from '@mui/system';

export const ContainerInline = styled('div')(() => ({
  flex: '1 1 auto',
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'center',
  marginTop: '8px',
}));
